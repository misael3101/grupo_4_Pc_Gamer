<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=, initial-scale=1.0">
    <title>registro</title>
    <link rel="stylesheet" href="/css/register.css">
    <script src="https://kit.fontawesome.com/e666ffac4e.js" crossorigin="anonymous"></script>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Roboto&display=swap" rel="stylesheet">
    <script src="//code.tidio.co/mloqng3fmqeerxntrqhe9y4kcjalrr5u.js" async></script>
</head>
<body>

<!--Header-->
<%- include("partials/header.ejs") %>

<!--main del register-->   

<main>

    <video autoplay muted  id="myVideo">
        <source src="./img/videoplayback.mp4" type="video/mp4">
        Tu navegador no soporta el tag de video.
      </video>    
    

        <div class="container">


                <form action="/usuario/register" class="formulario" method="post" enctype="multipart/form-data" >
                    <h2 class="titulo">REGISTRARSE</h2>
                        <ul class="errores">

                        </ul>
                    <div class="div_padre">

                        <!--nombre-->
                        <div class="nombre">
                            <label for="nombre">Nombre completo</label>
                            <input type="text" 
                                   name="nombre"
                                   id="nombre" 
                                   class="nombre"  
                                   placeholder="Juan"
                                   value="<%= locals.oldData ? oldData.nombre : null %>">
                                   
                            <% if (locals.errors && errors.nombre) { %>
                                <div class="mensaje-error">
                                    <%= errors.nombre.msg %>
                                </div>
                            <% } %>
                        </div>
    
                        <!--apellido-->
                        <div class="apellido">
                            <label for="apellido">Apellido</label>
                            <input type="text" 
                                   name="apellido" 
                                   id="apellido" 
                                   placeholder="Perez"
                                   value="<%= locals.oldData ? oldData.apellido : null %>">
                            <% if (locals.errors && errors.apellido) { %>
                                <div class="mensaje-error">
                                    <%= errors.apellido.msg %>
                                </div>
                            <% } %>
                        </div>
                        <!--correo-->
                        <div class="correo">
                            <label for="correo">Correo</label>
                            <input type="email" 
                                   name="correo" 
                                   id="correo" 
                                   placeholder="juanperez@gmail.com"
                                   value="<%= locals.oldData ? oldData.correo : null %>">
                            <% if (locals.errors && errors.correo) { %>
                                <div class="mensaje-error">
                                    <%= errors.correo.msg %>
                                </div>
                            <% } %>
                        </div>
                        <!--contraseña-->
                        <div class="contraseña">
                            <label for="contraseña">Contraseña</label>
                            <input type="password" 
                                   name="contrasena" 
                                   id="contrasena" 
                                   placeholder="******"
                                   value="<%= locals.oldData ? oldData.contrasena : null %>">
                            <% if (locals.errors && errors.contrasena) { %>
                                <div class="mensaje-error">
                                    <%= errors.contrasena.msg %>
                                </div>
                            <% } %>
                        </div>
                        <div class="cumpleanos">
                            <label for="clumpeanos">Fecha de nacimiento</label>
                            <input type="date" 
                                   name="cumpleanos" 
                                   id="cumpleanos" 
                                   placeholder="juanperez@gmail.com"
                                   value="<%= locals.oldData ? oldData.cumpleanos : null %>">
                            <% if (locals.errors && errors.cumpleanos) { %>
                                <div class="mensaje-error">
                                    <%= errors.cumpleanos.msg %>
                                </div>
                            <% } %>
                        </div>
                        <!--Imagen-->
                        <label for="imagendeusuario" class="form-label">Imagen de perfil:</label>
                        <input type="file" id="product-image" name="imagen">
                        <% if (locals.errors && errors.imagen) { %>
                            <div class="mensaje-error">
                                <%= errors.imagen.msg %>
                            </div>
                        <% } %>
                        <!--Categoria-->
                        <label for="categoria" class="form-label">Genero:</label>
                        <select name="genero" id="category" class="form-input">
                            <option value="">Elegí</option>
                            <option  
                                value="Masculino">Masculino</option>
                            <option value="Femenino" >Femenino</option>
                        </select>
                        <% if (locals.errors && errors.genero) { %>
                            <div class="mensaje-error">
                                <%= errors.genero.msg %>
                            </div>
                        <% } %>
                        <input type="submit" class="boton-enviar" value="Registrarte">
                        <!--en caso de que ya tenga cuenta-->
                        <div class="cuenta">
                            <p>ya tienes una cuenta? <a href="/usuario/login">toca aqui</a></p>
                        </div>
                    </div>
                </form>
            </div>
    </main>

    <script src="/js/register.js"></script>
    
    <footer class="footer-color">
        <ul>
            <div class="contenedor">
                <h2 class="titulo-seccion">Contactanos</h2>
            </div>
            <div class="redes-sociales">
                <i class ="fab fa-twitter"></i>
                <i class="fab fa-facebook"></i>
                <i class="fab fa-whatsapp"></i>
                <i class="fab fa-instagram"></i>
            </div>
            <div class="contenedor">
                <p class="copy">2023Grupo4/Todos los derechos reservados.</p>
            </div>
    </footer>


    
</body>
</html>